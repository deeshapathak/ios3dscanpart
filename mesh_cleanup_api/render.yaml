services:
  - type: web
    name: mesh-cleanup-api
    runtime: docker
    plan: free  # or starter ($7/month) for better performance
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
    healthCheckPath: /
    autoDeploy: true

